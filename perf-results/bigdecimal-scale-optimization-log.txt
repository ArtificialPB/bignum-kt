=== BASELINE — 2026-04-23 00:17 ===
Validation source:
- `./gradlew macosArm64Test`
- `./gradlew :benchmarks:macosArm64BigDecimalScaleSmokeBenchmark --rerun-tasks --no-daemon`
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.17.29.945532/macosArm64.json`

Benchmark                                               small  / medium / large   us/op
--------------------------------------------------------------------------------------
movePointLeftMethod                                     0.026 / 0.024 / 0.024
movePointRightMethod                                    0.024 / 0.075 / 0.314
precisionMethod                                         0.205 / 0.798 / 4.420
scaleByPowerOfTenMethod                                 0.024 / 0.024 / 0.025
scaleMethod                                             0.005 / 0.005 / 0.005
setScaleExactMethod                                     0.069 / 0.080 / 0.099
setScaleRoundedMethod                                   0.680 / 0.947 / 1.565
signumMethod                                            0.006 / 0.006 / 0.006
stripTrailingZerosMethod                                0.416 / 0.220 / 1.122
unscaledValueMethod                                     0.004 / 0.004 / 0.004

Initial target:
- `setScaleRounded()` is the clearest hot path. The current native `divideByPowerOfTen()`
  always materializes `10^n` and calls generic `mp_div`, even when the dropped scale is a
  single decimal digit and the divisor fits in one LibTomMath limb.
- `stripTrailingZeros()` is the next best follow-on target because it shares the same
  decimal-division primitive and also spends measurable time on larger magnitudes.
- JDK reference inspected from local source: `jdk1.8.0_161` `java/math/BigDecimal.java`
  (`setScale`, `movePointLeft`, `movePointRight`, `scaleByPowerOfTen`, `stripTrailingZeros`,
  and the zero-stripping helpers).

=== RUN 2 — 2026-04-23 00:20 ===
Change: Added a small-power fast path to native `divideByPowerOfTen()`. When `10^n` fits in one
LibTomMath digit, the native path now uses `mp_div_d` directly and computes the half-divisor
comparison from the digit remainder instead of materializing a full `10^n` `mp_int` and routing
through generic `mp_div`.

Validation source:
- `./gradlew macosArm64Test`
- `./gradlew :benchmarks:macosArm64BigDecimalScaleSmokeBenchmark --rerun-tasks --no-daemon`
- Confirmation rerun of the same smoke benchmark produced the same direction and similar magnitudes
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.20.45.888355/macosArm64.json`
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.22.29.663704/macosArm64.json`

Key deltas versus baseline:
- `setScaleRoundedMethod`:    0.680 / 0.947 / 1.565 -> 0.242 / 0.302 / 0.566 us/op
- Confirmation rerun:         0.680 / 0.947 / 1.565 -> 0.233 / 0.309 / 0.583 us/op
- `movePointRightMethod`:     0.024 / 0.075 / 0.314 -> 0.025 / 0.074 / 0.311 us/op
- `stripTrailingZerosMethod`: 0.416 / 0.220 / 1.122 -> 0.428 / 0.204 / 1.117 us/op

Observed effect:
- The rounded rescale path improved by roughly 64-66% on small, 67-68% on medium, and 63-64% on
  large across the two reruns. This matches the intended scope: the benchmark drops exactly one
  decimal digit, so it now stays in the single-digit LibTomMath division path instead of paying
  for dynamic `10^n` construction.
- `movePointRight()` picked up a small incidental win on the medium and large profiles because the
  same helper backs decimal truncation in related code paths.
- `stripTrailingZeros()` stayed effectively flat overall, which keeps the optimization tightly
  focused on rounded scale reduction instead of perturbing unrelated decimal helpers.

=== RUN 3 — 2026-04-23 00:27 ===
Change: Reworked native `stripTrailingZeros()` to mirror the JDK helper shape more closely. The
loop now rejects odd unscaled values immediately and uses the `mp_div_d`-backed digit division
helper for the remaining decimal-zero checks instead of routing through full BigInteger `% 10` and
`/ 10` operations.

Validation source:
- `./gradlew macosArm64Test`
- `./gradlew :benchmarks:macosArm64BigDecimalScaleSmokeBenchmark --rerun-tasks --no-daemon`
- Confirmation rerun of the same smoke benchmark produced the same direction and similar magnitudes
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.27.12.206321/macosArm64.json`
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.28.59.270807/macosArm64.json`

Key deltas versus previous committed run:
- `stripTrailingZerosMethod`: 0.428 / 0.204 / 1.117 -> 0.259 / 0.029 / 0.029 us/op
- Confirmation rerun:        0.436 / 0.217 / 1.115 -> 0.261 / 0.029 / 0.029 us/op

Observed effect:
- The small profile improved by about 39-40% because the exact-zero-stripping branch now does its
  single decimal division through the digit helper instead of the generic BigInteger operators.
- The medium and large profiles improved by roughly 86-87% and 97% because their unscaled values
  are odd in this fixture, so the native method now exits on the cheap low-limb parity check
  without attempting any decimal division at all.
- The rest of the suite moved around in the same broad band as the previous reruns, which fits the
  scope: this change only touches `stripTrailingZeros()` and does not perturb the shared rescale
  helpers.

=== RUN 4 — 2026-04-23 01:00 ===
Change: Removed the native `precision()` dependency on decimal string rendering. `BigDecimal`
now caches the computed precision, and the uncached computation uses a JDK-style decimal digit
estimate from the unscaled magnitude bit length plus a single compare against `10^r`, matching
the shape of `BigDecimal.bigDigitLength(...)` instead of calling `unscaled.abs().toString().length`.

Validation source:
- `./gradlew macosArm64Test`
- `./gradlew :benchmarks:macosArm64BigDecimalScaleSmokeBenchmark --rerun-tasks --no-daemon`
- Confirmation rerun of the same smoke benchmark produced the same direction and similar magnitudes
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T00.59.42.534444/macosArm64.json`
- `benchmarks/build/reports/benchmarks/bigDecimalScaleSmoke/2026-04-23T01.01.28.823924/macosArm64.json`

Key deltas versus previous committed run:
- `precisionMethod`:         0.241 / 0.900 / 5.312 -> 0.007 / 0.007 / 0.012 us/op
- Confirmation rerun:        0.241 / 0.900 / 5.312 -> 0.007 / 0.007 / 0.012 us/op
- `setScaleRoundedMethod`:   0.261 / 0.305 / 0.574 -> 0.276 / 0.376 / 0.674 us/op
- `stripTrailingZerosMethod`: 0.259 / 0.029 / 0.029 -> 0.263 / 0.029 / 0.029 us/op

Observed effect:
- Repeated `precision()` reads are now effectively constant-time on this fixture because the native
  object memoizes the result after the first computation. The two reruns both land at roughly
  97% faster on the small profile, 99% faster on medium, and nearly 100% faster on the large
  profile relative to the uncached `toString()` path.
- The benchmark harness uses a benchmark-scoped fixture, so `precisionMethod()` mostly measures the
  cached steady state. That means these numbers validate the cache hit path directly, while the
  code change to a JDK-style bit-length estimate removes the remaining decimal-string allocation
  from the first uncached precision computation.
- The rest of the scale smoke suite stayed in the usual rerun band. `stripTrailingZeros()` stayed
  flat, and `setScaleRounded()` drifted upward from the unusually low RUN 3 sample but remained
  well ahead of the original baseline after the earlier single-digit division optimization.
